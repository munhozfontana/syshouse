import 'package:flutter/material.dart';

class SocioEdit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("SocioEdit"),
    );
  }
}
